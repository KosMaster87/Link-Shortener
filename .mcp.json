{
  "mcpServers": {
    "db": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "${DATABASE_URL:-postgresql:///linkshort?host=/var/run/postgresql}"
      ],
      "transport": "stdio"
    }
  }
}
