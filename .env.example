# LinkShort Environment-Variablen
# Diese Datei nach .env kopieren und Werte anpassen.

# Server
PORT=3000
NODE_ENV=development

# Datenbank
# Option A (empfohlen für Managed DB in Production):
# DATABASE_URL=postgresql://user:pass@host:5432/linkshort

# Option B (lokal/alternativ):
PGHOST=/var/run/postgresql
PGPORT=5432
PGDATABASE=linkshort
PGUSER=dev2k
PGPASSWORD=

# Auth (required)
JWT_SECRET=replace-with-a-long-random-string
SESSION_EXPIRY=86400

# Optional
ANTHROPIC_API_KEY=sk-ant-dein-key-hier
LOG_LEVEL=info
RATE_LIMIT_MAX=100
